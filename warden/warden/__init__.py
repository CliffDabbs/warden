"""Warden — a first-class web hub for parental controls.

Web UI + rules engine + pluggable data sources, controlling AdGuard Home directly.
A pivot/merge of two prior ideas in this repo: the AdGuard "screentime" IaC and the
multi-source digest platform (see ../docs). Home Assistant is no longer in the loop.
"""

__version__ = "0.1.0"
