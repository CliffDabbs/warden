"""Amazon Kids Parent Dashboard — session capture and endpoint recon (no adapter yet).

Only `login.py` lives here so far: Warden imports an adapter package only when
`config/warden.yaml` names it as a source, so this one is inert until there is
something worth wiring up. See `login.py` for what the recon is trying to answer.
"""
